export default function Dashboard() {
  return (
    <div style={{ height: "100%" }}>
      {/* Dashed placeholder content area — matches the reference UI */}
      <div
        style={{
          minHeight: "calc(100vh - 112px)",
          backgroundColor: "#ffffff",
          border: "2px dashed #d1d5db",
          borderRadius: 12,
        }}
      />
    </div>
  );
}
